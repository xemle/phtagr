Requirements:

- php >= 4
- mySQL >= 4.1
- php-mysql
- imagemagick

For installation read INSTALL

Source Style:

- 2 Spaces, no tabs (php and html output)
- Private functions have a prefix of an underscore ("_")
- Documentation style of doxygen
- Valid php 4 code

Patches:

- Send patches from the currenct subversion repository to sebastian@silef.de.

Commits:

- Add "- " on comments at a commit
